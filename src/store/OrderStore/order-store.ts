import {action, makeObservable, observable} from 'mobx';import {ApiOrderType, ordersApi, SendDataOrderType, StatusType} from "../../api/ordersApi";export class OrderStore {    order: ApiOrderType = {} as ApiOrderType    orders: ApiOrderType[] = [] as ApiOrderType[]    statusOrder: StatusType | null = null    setOrderData(order: ApiOrderType): void {        this.order = order    };    setStatus(status: StatusType): void {        this.statusOrder = status    };    setOrders(orders: ApiOrderType[]): void {        this.orders = orders    };    async getOrders(userId: string,status?:StatusType) {        const {data} = await ordersApi.getOrderUserId(userId, status)        this.setOrders(data.results)    }    async sendOrder(order: SendDataOrderType) {        const {data} = await ordersApi.postOrders(order)        this.setOrderData(data)        this.setStatus(data.status)    }    constructor() {        makeObservable(this, {            order: observable,            orders: observable,            statusOrder: observable,            setOrderData: action,            setStatus: action,            setOrders: action,            sendOrder: action,        });        this.setOrderData = this.setOrderData.bind(this)        this.setOrders = this.setOrders.bind(this)        this.setStatus = this.setStatus.bind(this)    }}export default new OrderStore();