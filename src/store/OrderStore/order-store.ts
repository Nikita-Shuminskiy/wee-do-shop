import {action, makeObservable, observable} from 'mobx';import {ordersApi, ResponseOrderType, SendDataOrderType, StatusType} from "../../api/ordersApi";export class OrderStore {    order: ResponseOrderType = {} as ResponseOrderType    statusOrder: StatusType | null = null    setOrderData(order: ResponseOrderType): void {        this.order = order    };    setStatus(status: StatusType): void {        this.statusOrder = status    };    async sendOrder(order: SendDataOrderType) {        const {data} = await ordersApi.postOrders(order)        this.setOrderData(data)        this.setStatus(data.status)    }    constructor() {        makeObservable(this, {            order: observable,            statusOrder: observable,            setOrderData: action,            setStatus: action,            sendOrder: action,        });        this.setOrderData = this.setOrderData.bind(this)        this.setStatus = this.setStatus.bind(this)    }}export default new OrderStore();