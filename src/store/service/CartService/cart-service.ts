import RootStore from "../../RootStore/root-store"import {ProductType} from "../../../api/productApi"import {StoreType} from "../../../api/storesApi"export class CartService {	rootStore: typeof RootStore saveProductToCart(		currentProduct: ProductType,		productValue: number,		store: StoreType	){		if (			this.rootStore.CartStore.cart?.idStore &&			this.rootStore.CartStore.cart?.idStore !== store._id		) {			this.rootStore.CartStore.shoppingCartMatching(				currentProduct,				productValue,				store,				this.rootStore.Notification.navigation			)			return		}		this.rootStore.CartStore.saveProductToCarts(currentProduct, productValue, store)	}	constructor(rootStore: typeof RootStore) {		this.rootStore = rootStore	}}export default CartService